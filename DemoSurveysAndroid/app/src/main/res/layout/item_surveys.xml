<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/list_row_selector"
    android:padding="8dp" >

    <!-- Thumbnail Image -->
    <ImageView
        android:id="@+id/thumbnail"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_alignParentLeft="true"
        android:layout_marginRight="8dp"
        android:src="@mipmap/ic_launcher"/>

    <!-- Movie Title -->
    <TextView
        android:id="@+id/name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/thumbnail"
        android:layout_toRightOf="@+id/thumbnail"
        android:textSize="@dimen/title"
        android:text="@string/app_name"
        android:textColor="#000000"
        android:textStyle="bold" />

    <!-- Rating -->
    <TextView
        android:id="@+id/description"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/name"
        android:layout_marginTop="1dip"
        android:layout_toRightOf="@+id/thumbnail"
        android:textColor="#504949"
        android:textSize="@dimen/rating"
        android:text="description"/>

    <!-- Genre -->
    <TextView
        android:id="@+id/surveyid"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/description"
        android:layout_marginTop="5dp"
        android:layout_toRightOf="@+id/thumbnail"
        android:textColor="@color/dark_grey"
        android:textSize="@dimen/genre"
        android:text="surveyID"/>

    <!-- Release Year -->
    <TextView
        android:id="@+id/lastupdated"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/light_grey"
        android:textSize="@dimen/year"
        android:text="lastupdated"
        android:layout_below="@+id/surveyid"
        android:layout_alignLeft="@+id/surveyid"
        android:layout_alignStart="@+id/surveyid"/>
    <TextView
        android:id="@+id/isOnline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="@dimen/year"
        android:text="IsOnline"
        android:layout_below="@+id/lastupdated"
        android:layout_alignLeft="@+id/lastupdated"
        android:layout_alignStart="@+id/lastupdated"/>

</RelativeLayout>